/**
 * @component Cargando
 * @description Componente simple que muestra un indicador de carga.
 * Se utiliza para mostrar un estado de carga mientras se esperan datos
 * o se realizan operaciones asíncronas.
 * 
 * @returns {JSX.Element} Retorna un div con el texto "Cargando"
 */
const Cargando = () => {
  return <div className="cargando">Cargando</div>;
};

export default Cargando;
